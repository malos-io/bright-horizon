steps:
  # ============================================
  # BACKEND
  # ============================================

  # Step 1: Build backend image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:$COMMIT_SHA'
      - '-t'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:latest'
      - './backend'
    id: 'build-backend'

  # Step 2: Push backend image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:$COMMIT_SHA'
    id: 'push-backend'
    waitFor: ['build-backend']

  # Step 3: Deploy backend to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'training-backend'
      - '--image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:$COMMIT_SHA'
      - '--region=asia-southeast1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--set-env-vars=ENVIRONMENT=production,ZOHO_REDIRECT_URI=https://api.brighthii.com/api/zoho/callback,ADMIN_FRONTEND_URL=https://portal.brighthii.com,PUBLIC_API_URL=https://api.brighthii.com,GCS_BUCKET=brighthii_prod'
      - '--set-secrets=ZOHO_CLIENT_ID=zoho_oauth_client_id:latest,ZOHO_CLIENT_SECRET=zoho_oauth_client_secret:latest,JWT_SECRET_KEY=zoho_oauth_jwt_secret:latest,ZOHO_MAIL_SELF_CLIENT_ID=zoho_self_client_id:latest,ZOHO_MAIL_SELF_SECRET_ID=zoho_self_client_secret:latest,ZOHO_MAIL_REFRESH_TOKEN=zoho_self_client_refresh_token:latest'
      - '--service-account=backend-runtime@brighthii.iam.gserviceaccount.com'
    id: 'deploy-backend'
    waitFor: ['push-backend']

  # ============================================
  # FRONTEND-PUBLIC
  # ============================================

  # Step 5: Build frontend-public image
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        docker build \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:$COMMIT_SHA \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:latest \
          --build-arg BUILD_SCRIPT=build:prod \
          --target cloudrun \
          ./frontend-public
    id: 'build-frontend-public'
    waitFor: ['-']

  # Step 6: Push frontend-public image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:$COMMIT_SHA'
    id: 'push-frontend-public'
    waitFor: ['build-frontend-public']

  # Step 7: Deploy frontend-public to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'training-frontend-public'
      - '--image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:$COMMIT_SHA'
      - '--region=asia-southeast1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=256Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=10'
    id: 'deploy-frontend-public'
    waitFor: ['push-frontend-public']

  # ============================================
  # FRONTEND-ADMIN
  # ============================================

  # Step 8: Build frontend-admin image (fetch Zoho client ID from Secret Manager)
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    secretEnv: ['ZOHO_CLIENT_ID']
    args:
      - '-c'
      - |
        docker build \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:$COMMIT_SHA \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:latest \
          --build-arg BUILD_SCRIPT=build:prod \
          --build-arg VITE_ZOHO_CLIENT_ID=$$ZOHO_CLIENT_ID \
          --target cloudrun \
          ./frontend-admin
    id: 'build-frontend-admin'
    waitFor: ['-']

  # Step 9: Push frontend-admin image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:$COMMIT_SHA'
    id: 'push-frontend-admin'
    waitFor: ['build-frontend-admin']

  # Step 10: Deploy frontend-admin to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'training-frontend-admin'
      - '--image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:$COMMIT_SHA'
      - '--region=asia-southeast1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=256Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=10'
    id: 'deploy-frontend-admin'
    waitFor: ['push-frontend-admin']

  # ============================================
  # FRONTEND-STUDENT
  # ============================================

  # Step 11: Build frontend-student image
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        docker build \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:$COMMIT_SHA \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:latest \
          --build-arg BUILD_SCRIPT=build:prod \
          --target cloudrun \
          ./frontend-student
    id: 'build-frontend-student'
    waitFor: ['-']

  # Step 12: Push frontend-student image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:$COMMIT_SHA'
    id: 'push-frontend-student'
    waitFor: ['build-frontend-student']

  # Step 13: Deploy frontend-student to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'training-frontend-student'
      - '--image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:$COMMIT_SHA'
      - '--region=asia-southeast1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=256Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=10'
    id: 'deploy-frontend-student'
    waitFor: ['push-frontend-student']


images:
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:$COMMIT_SHA'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-backend-prod:latest'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:$COMMIT_SHA'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-public-prod:latest'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:$COMMIT_SHA'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-admin-prod:latest'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:$COMMIT_SHA'
  - 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/training-center/training-frontend-student-prod:latest'

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/zoho_oauth_client_id/versions/latest
      env: 'ZOHO_CLIENT_ID'

options:
  logging: CLOUD_LOGGING_ONLY
